<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.piedrazul.frontend.controller.AdministradorDashboardController"
            stylesheets="@../css/dashboard.css"
            styleClass="dashboard-root"
            prefWidth="1200"
            prefHeight="720">

    <!-- BARRA SUPERIOR -->
    <top>
        <HBox styleClass="top-bar" alignment="CENTER_LEFT">

            <Label text="PIEDRAZUL" styleClass="logo-label"/>

            <Region HBox.hgrow="ALWAYS"/>

            <Button text="Cerrar sesión"
                    onAction="#handleLogout"
                    styleClass="logout-button"/>

            <StackPane styleClass="icon-box">
                <Label text="🔔" styleClass="notification-icon"/>
            </StackPane>

        </HBox>
    </top>

    <!-- CONTENIDO -->
    <center>
        <VBox styleClass="dashboard-content">

            <!-- Fila de bienvenida y rol -->
            <HBox styleClass="dashboard-info-row" alignment="CENTER_LEFT">

                <Label fx:id="lblBienvenida"
                       text="Bienvenido"
                       styleClass="welcome-label"/>

                <Region HBox.hgrow="ALWAYS"/>

                <Label text="Administrador"
                       styleClass="role-text"/>

            </HBox>

            <!-- Zona central de botones -->
            <StackPane styleClass="main-actions-container" VBox.vgrow="ALWAYS">

                <VBox alignment="CENTER"
                      spacing="55"
                      styleClass="main-actions-grid">

                    <!-- Fila superior -->
                    <HBox alignment="CENTER"
                          spacing="55"
                          styleClass="main-actions-row">

                        <Button text="REGISTRAR USUARIO"
                                styleClass="dashboard-main-button"
                                onAction="#handleRegistrarUsuario"/>

                        <Button text="ADMINISTRAR USUARIOS"
                                styleClass="dashboard-main-button"
                                onAction="#handleAdministrarUsuarios"/>

                    </HBox>

                    <!-- Fila inferior -->
                    <HBox alignment="CENTER"
                          spacing="55"
                          styleClass="main-actions-row">

                        <Button text="CONFIGURAR FESTIVOS"
                                styleClass="dashboard-main-button"
                                onAction="#handleConfigurarFestivos"/>

                        <Button text="CONFIGURACIÓN DE&#10;HORARIOS"
                                styleClass="dashboard-main-button"
                                onAction="#handleConfiguracionHorarios"/>

                    </HBox>

                    <!-- Fila reportes -->
                    <HBox alignment="CENTER"
                          styleClass="main-actions-row">

                        <Button text="REPORTES"
                                styleClass="dashboard-main-button"
                                onAction="#handleReportes"/>

                    </HBox>

                </VBox>

            </StackPane>

            <!-- Texto inferior -->
            <HBox alignment="CENTER_RIGHT" styleClass="footer-row">
                <Label text="Menú principal"
                       styleClass="menu-label"/>
            </HBox>

        </VBox>
    </center>

</BorderPane>